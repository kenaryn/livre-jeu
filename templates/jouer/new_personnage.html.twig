{% extends 'base.html.twig' %}
{%  block title %}Créer un nouveau personnage{% endblock %}
{% block body %}

{{ form_start(form) }}
{{ form_widget(form) }}
<div class="d-flex justify-content-end">
  <a class="btn btn-primary me-3" href="{{ path('app_jouer')}}">Retour</a>
  <button class="btn btn-primary">Créer</button>
  
</div>
{{ form_end(form) }}
{% endblock %}